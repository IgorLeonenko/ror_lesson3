.row
  .col-md-8.bs-callout
    %h1 New Post
    = form_for @post, :url => user_posts_path(current_user.id, @post.id) do |f|
      = render partial: "form", locals: { :f => f }
      = link_to ' back', :back, class: "btn btn-info fa fa-circle-o-notch"
